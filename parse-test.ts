import { Document, Paragraph, TextRun, HeadingLevel, Table, TableRow, TableCell, ImageRun, Packer } from "docx";

export function htmlToDocxElements(html: string) {
    // We would use DOMParser in browser
}
